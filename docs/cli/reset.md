---
summary: "CLI reference for `zoidbergbot reset` (reset local state/config)"
read_when:
  - You want to wipe local state while keeping the CLI installed
  - You want a dry-run of what would be removed
title: "reset"
---

# `zoidbergbot reset`

Reset local config/state (keeps the CLI installed).

```bash
zoidbergbot reset
zoidbergbot reset --dry-run
zoidbergbot reset --scope config+creds+sessions --yes --non-interactive
```
