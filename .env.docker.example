# OpenClaw Docker Environment Configuration
# Copy this file to .env and customize for your environment

# ==================================================================
# Container Image
# ==================================================================
OPENCLAW_IMAGE=openclaw:local

# ==================================================================
# Gateway Configuration
# ==================================================================
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan

# ==================================================================
# Data Directories (Host Paths)
# ==================================================================
# All OpenClaw data will be stored in these directories on the host
# For Synology/NAS: use paths like /docker/openclaw/data
# For standard Linux: use paths like ./data
OPENCLAW_CONFIG_DIR=./data
OPENCLAW_WORKSPACE_DIR=./data/workspace

# ==================================================================
# Authentication & API Keys
# ==================================================================
# Generate a secure random token: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Optional Claude.ai provider credentials
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=

# ==================================================================
# Optional: Additional Packages
# ==================================================================
# Extra apt packages to install in Docker image
# OPENCLAW_DOCKER_APT_PACKAGES="ffmpeg build-essential"
