services:
  - type: web
    name: openclaw
    runtime: docker
    plan: standard
    healthCheckPath: /health
    dockerCommand: sh -c "mkdir -p /data/.openclaw && rm -f /data/.openclaw/*.json && echo '{\"gateway\":{\"controlUi\":{\"dangerouslyDisableDeviceAuth\":true}}}' > /data/.openclaw/openclaw.json && node dist/index.js gateway --bind lan --port 8080"
    envVars:
      - key: PORT
        value: "8080"
      - key: SETUP_PASSWORD
        sync: false
      - key: OPENCLAW_STATE_DIR
        value: /data/.openclaw
      - key: OPENCLAW_WORKSPACE_DIR
        value: /data/workspace
      - key: OPENCLAW_GATEWAY_PASSWORD
        sync: false
    disk:
      name: openclaw-data
      mountPath: /data
      sizeGB: 1
